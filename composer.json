{
	"name" : "socialanalytics",
	"description" : "Social Analytics One",
	"require": {
		"php": ">=5.3.2",
		"codeigniter/framework": "3.1.*",
        "composer/installers": "~1.0"
	},
	"scripts": {
        "post-update-cmd": [
            "npm install --production",
            "bower install --production",
            "gulp --production"
        ],
        "post-install-cmd": [
            "npm install --production",
            "bower install --production",
            "gulp --production"
        ]
    }
}
